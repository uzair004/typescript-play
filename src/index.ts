export = {}

let score = 0;
let age:number;

let name = 'Uzair';

let numbers:number[] = [1,2,3,4,5];

let Mixed = [1, "2"]

function calculateTax(amount:number):number {
    let x;
    return amount * 0.08;
}